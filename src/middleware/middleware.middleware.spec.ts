import { MiddlewareMiddleware } from './tenant.middleware';

describe('MiddlewareMiddleware', () => {
  it('should be defined', () => {
    expect(new MiddlewareMiddleware()).toBeDefined();
  });
});
